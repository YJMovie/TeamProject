<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<!-- resultType = "DB의 레코드와 자료저장 클래스 mapping" -->
	<!-- <select id="list" resultType="join.joinDTO">
		select * from join order by desc
	</select> -->
	
	<insert id="insert">
		insert into member_info values (
		  #{userid},#{userpwd},#{name},#{gender},#{phone},#{address},
		  #{email},sysdate,2
		)
	</insert>
	<select id="confirmID" parameterType="String" resultType="String">
		select userid from member_info where userid = #{userid}
	</select>
	<select id="memlist" resultType="member.dto.MemberDTO">
		select aa.* from
		 	(
	    		(select rownum as rn, mem.*  from
	     			( select  * from member_info order by name desc  )
	     		 mem) 
     		aa)
		where 
			rn between #{start} and #{end}
			
	</select>
	<delete id="delete">
		delete from member_info where userid=#{userid}
	</delete>
	<update  id="memUpdate">
		update member_info
		set
			userpwd = #{userpwd},
			name = #{name},
			phone = #{phone},
			address = #{address},
			gender = #{gender},
			email = #{email}
		where
			userid = #{userid}
	</update>
	<update  id="admin_memUpdate">
		update member_info
		set
			userpwd = #{userpwd},
			name = #{name},
			phone = #{phone},
			address = #{address},
			gender = #{gender},
			email = #{email},
			usergrade = #{usergrade}
		where
			userid = #{userid}
	</update>
	<select id="memGet" resultType="member.dto.MemberDTO">
		select * from member_info where userid = #{userid}
	</select>
	<select id="recordCount" resultType="int">
		select count(*) as recordCount from member_info
	</select>
	<select id="memberGrade" parameterType="String" resultType="int">
		select usergrade from member_info where name = #{name}
	</select>
	<insert id="favoriteMovie">
		insert into member_code (mem_userid, mem_mvcode) values 
		(#{mem_userid},#{mem_mvcode})
	</insert>
</mapper>